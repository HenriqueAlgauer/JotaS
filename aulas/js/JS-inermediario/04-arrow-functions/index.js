let resulatdoSoma = (numero1, numero2) => numero1 + numero2
console.log(resulatdoSoma(12, 4));

const incentivarQuester = nomeQuester => {
    console.log(`Parabens ` + nomeQuester + `, voce chegou ao modulo de JS intermediario`)
}

incentivarQuester('Henrique')
